import { OffsetDirective } from './offset.directive';

describe('OffsetDirective', () => {
  it('should create an instance', () => {
    const directive = new OffsetDirective();
    expect(directive).toBeTruthy();
  });
});
