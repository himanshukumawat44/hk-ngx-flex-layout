import { LayoutAlignDirective } from './layout-align.directive';

describe('LayoutAlignDirective', () => {
  it('should create an instance', () => {
    const directive = new LayoutAlignDirective();
    expect(directive).toBeTruthy();
  });
});
