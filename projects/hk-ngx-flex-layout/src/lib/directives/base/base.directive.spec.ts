import { BaseDirective } from './base.directive';

describe('BaseLayoutDirectiveDirective', () => {
  it('should create an instance', () => {
    // const directive = new BaseDirective();
    // expect(directive).toBeTruthy();
  });
});
