import { GapDirective } from './gap.directive';

describe('GapDirective', () => {
  it('should create an instance', () => {
    const directive = new GapDirective();
    expect(directive).toBeTruthy();
  });
});
